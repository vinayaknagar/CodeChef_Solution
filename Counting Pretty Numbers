# cook your dish here Vinayak Nagar
for _ in range(int(input())):
    a,b=map(int,input().split(" "))
    count=0
    for i in range(a,b+1):
        c=str(i)
        
        if c[len(c)-1]== "2" or c[len(c)-1]== "3" or c[len(c)-1]== "9":
            count+=1
    print(count)
